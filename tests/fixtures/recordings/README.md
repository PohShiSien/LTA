# Recording parser fixtures

Small source excerpts from the user-supplied PS3 datasets. CSV files retain the first four physical lines; ACV worksheets retain their exact original headers and first two data rows. The rich ACV excerpt preserves all 483 columns. These are schema/parser tests, not training examples or validation outcomes. Rail tests repeat recorded sample rows only to exercise its strict 10,000-row input contract.
